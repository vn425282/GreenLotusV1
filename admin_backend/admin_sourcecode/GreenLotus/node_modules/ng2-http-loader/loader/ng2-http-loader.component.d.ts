import { OnInit } from '@angular/core';
import { Ng2HttpLoaderEmitter } from './ng2-http-loader-emitter';
export declare class Ng2HttpLoaderComponent implements OnInit {
    private emitter;
    private subscriber;
    color: string;
    private progress;
    private totalRequests;
    private visibility;
    private thresHold;
    private anim1;
    private anim2;
    constructor(emitter: Ng2HttpLoaderEmitter);
    ngOnInit(): void;
    private startLoader(action?);
    handleProgressPercentage(flag: boolean): void;
    private show();
    private hide();
}
